{
  "name": "famil-ia-functions",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:rules": "firebase emulators:exec --project demo-famil-ia-rules --only firestore \"vitest run --config vitest.rules.config.ts\"",
    "test:smoke": "vitest run --config vitest.smoke.config.ts",
    "test:watch": "vitest",
    "deploy": "firebase deploy --only functions",
    "serve": "npm run build && firebase emulators:start --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^6.1.0"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "@types/node": "^20.14.0",
    "firebase": "^12.12.1",
    "typescript": "^5.6.0",
    "vitest": "^3.0.5"
  }
}
