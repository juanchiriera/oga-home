rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Cerrado por defecto. Rutas típicas v1: recibos/audio por familia con validación en reglas o vía signed URLs + Functions.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
